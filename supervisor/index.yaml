---
default:
  name: supervisor
  version: "3.1.3"
  release: 3
  pypi: supervisor
  summary: A system for allowing the control of process state on UNIX.
  python: /usr/bin/python

  requires:
    - python-meld3
